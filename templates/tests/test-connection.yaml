apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hello-nginx.fullname" . }}-test-connection"
  labels:
    {{- include "hello-nginx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hello-nginx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
